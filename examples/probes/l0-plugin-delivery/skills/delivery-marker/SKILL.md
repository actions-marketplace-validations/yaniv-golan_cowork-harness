---
name: delivery-marker
description: Marker skill whose presence in the agent's init inventory proves the plugin under test was actually delivered at protocol fidelity.
---

The body is irrelevant. This skill exists so a scenario can assert that `--plugin-dir` delivered it.
